# تحديثات لوحة الإدارة (Admin Panel Updates)

## نظرة عامة
تم إصلاح وتطوير نظام إدارة الكتب ونظام إرسال الإشعارات في لوحة الإدارة.

---

## 1. إصلاح صفحة إدارة الكتب (Admin Books Management)

### المشاكل التي تم إصلاحها:
- ✅ زر "إضافة كتاب جديد" لم يكن يعمل
- ✅ أزرار "تعديل" و "حذف" للكتب لم تكن تعمل
- ✅ عدم وجود واجهة لرفع صورة الغلاف

### الميزات الجديدة:

#### 1. مربع حوار إضافة/تعديل كتاب
- **الحقول المتاحة:**
  - صورة الغلاف (مع معاينة فورية)
  - العنوان بالعربية *
  - المؤلف بالعربية *
  - التصنيف * (قائمة منسدلة)
  - الوصف
  - السعر (ج.م) *
  - سعر الخصم (ج.م)
  - الكمية المتوفرة *
  - نسبة عمولة المسوقين (%)

#### 2. رفع صورة الغلاف
- **المواصفات:**
  - أنواع الملفات المقبولة: جميع صور (image/*)
  - الحد الأقصى للحجم: 2 ميجابايت
  - معاينة فورية قبل الحفظ
  - إمكانية إزالة الصورة المختارة

- **التخزين:**
  - يتم رفع الصور إلى Supabase Storage
  - Bucket: `book-covers`
  - الصور عامة (public) للعرض للمستخدمين

#### 3. التحقق من البيانات
- التحقق من ملء الحقول المطلوبة
- التحقق من نوع وحجم ملف الصورة
- رسائل خطأ واضحة بالعربية

#### 4. الوظائف المتاحة
- ✅ إضافة كتاب جديد
- ✅ تعديل كتاب موجود
- ✅ حذف كتاب (مع تأكيد)
- ✅ رفع صورة الغلاف
- ✅ معاينة الصورة قبل الحفظ

---

## 2. عرض الكتب بدون أغلفة

### التحديث:
عند عدم توفر صورة غلاف للكتاب، يتم عرض:
- أيقونة كتاب 📚
- نص: "الأغلفة قيد الرفع"
- خلفية رمادية (bg-muted)

### الصفحات المحدثة:
- ✅ الصفحة الرئيسية (Home)
- ✅ قائمة الأمنيات (Wishlist)

---

## 3. نظام إرسال الإشعارات الجماعية

### الميزة الجديدة:
تم إضافة زر "إرسال إشعار" في صفحة الإشعارات الإدارية يسمح للمدير بإرسال إشعارات مخصصة.

### مربع حوار إرسال الإشعار:

#### الحقول:
1. **الفئة المستهدفة * (Recipient Type)**
   - الجميع (مشترين ومسوقين)
   - المشترين فقط
   - المسوقين فقط

2. **عنوان الإشعار ***
   - مثال: "عرض خاص على جميع الكتب"

3. **نص الإشعار ***
   - نص طويل (Textarea)
   - مثال: "احصل على خصم 20% على جميع الكتب حتى نهاية الشهر"

### كيفية العمل:
1. المدير يضغط على زر "إرسال إشعار"
2. يختار الفئة المستهدفة
3. يكتب العنوان والنص
4. يضغط "إرسال"
5. النظام يقوم بـ:
   - جلب قائمة المستخدمين حسب الفئة المختارة
   - إنشاء إشعار لكل مستخدم
   - إدراج الإشعارات في قاعدة البيانات
   - عرض رسالة نجاح مع عدد المستخدمين الذين تم إرسال الإشعار لهم

### الأمان:
- الوظيفة متاحة للإدارة فقط
- التحقق من ملء جميع الحقول المطلوبة
- معالجة الأخطاء بشكل صحيح

---

## 4. التحديثات التقنية

### API Functions الجديدة:

#### 1. `uploadBookCover(file: File): Promise<string>`
```typescript
// رفع صورة غلاف كتاب إلى Storage
const coverUrl = await uploadBookCover(file);
```

#### 2. `sendBulkNotification(title, message, recipientType)`
```typescript
// إرسال إشعار جماعي
await sendBulkNotification(
  'عنوان الإشعار',
  'نص الإشعار',
  'all' | 'customers' | 'affiliates'
);
```

### Storage Buckets:

#### book-covers
- **الغرض:** تخزين صور أغلفة الكتب
- **الصلاحيات:**
  - الإدارة: رفع وحذف
  - الجميع: عرض الصور

```sql
-- السياسات
CREATE POLICY "Admins can upload book covers"
  ON storage.objects FOR INSERT
  WITH CHECK (
    bucket_id = 'book-covers' AND
    EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin')
  );

CREATE POLICY "Anyone can view book covers"
  ON storage.objects FOR SELECT
  USING (bucket_id = 'book-covers');

CREATE POLICY "Admins can delete book covers"
  ON storage.objects FOR DELETE
  USING (
    bucket_id = 'book-covers' AND
    EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin')
  );
```

---

## 5. واجهة المستخدم (UI/UX)

### التحسينات:
- ✅ مربعات حوار (Dialogs) احترافية مع shadcn/ui
- ✅ رسائل تحميل (Loading states) أثناء الحفظ/الإرسال
- ✅ رسائل نجاح/خطأ واضحة بالعربية
- ✅ معاينة فورية للصور
- ✅ تصميم متجاوب (Responsive)
- ✅ دعم RTL كامل

### الأيقونات المستخدمة:
- Plus: إضافة
- Edit: تعديل
- Trash2: حذف
- Upload: رفع
- X: إزالة
- Send: إرسال
- Loader2: تحميل

---

## 6. أمثلة الاستخدام

### إضافة كتاب جديد:
1. اذهب إلى "إدارة الكتب"
2. اضغط "إضافة كتاب جديد"
3. املأ البيانات:
   - اختر صورة الغلاف (اختياري)
   - العنوان: "الأمير الصغير"
   - المؤلف: "أنطوان دو سانت إكزوبيري"
   - التصنيف: "أدب"
   - السعر: 120 ج.م
   - الكمية: 50
4. اضغط "إضافة"

### تعديل كتاب:
1. اضغط على أيقونة التعديل ✏️ بجانب الكتاب
2. عدّل البيانات المطلوبة
3. اضغط "تحديث"

### حذف كتاب:
1. اضغط على أيقونة الحذف 🗑️ بجانب الكتاب
2. أكد الحذف

### إرسال إشعار:
1. اذهب إلى "الإشعارات"
2. اضغط "إرسال إشعار"
3. اختر الفئة: "المشترين فقط"
4. العنوان: "عرض خاص"
5. النص: "خصم 20% على جميع الكتب حتى نهاية الشهر"
6. اضغط "إرسال"

---

## 7. معالجة الأخطاء

### الأخطاء المحتملة ومعالجتها:

#### رفع صورة الغلاف:
- ❌ ملف ليس صورة → "يرجى اختيار ملف صورة"
- ❌ حجم أكبر من 2MB → "حجم الصورة يجب أن يكون أقل من 2 ميجابايت"
- ❌ فشل الرفع → "فشل رفع الصورة"

#### حفظ الكتاب:
- ❌ حقول مطلوبة فارغة → "يرجى ملء جميع الحقول المطلوبة"
- ❌ فشل الحفظ → "فشل حفظ الكتاب"

#### إرسال الإشعار:
- ❌ حقول فارغة → "يرجى ملء جميع الحقول"
- ❌ لا يوجد مستخدمين → "لا يوجد مستخدمين لإرسال الإشعار إليهم"
- ❌ فشل الإرسال → "فشل إرسال الإشعار"

---

## 8. الاختبارات

### تم اختبار:
- ✅ إضافة كتاب جديد مع صورة غلاف
- ✅ إضافة كتاب بدون صورة غلاف
- ✅ تعديل كتاب موجود
- ✅ تعديل صورة الغلاف
- ✅ حذف كتاب
- ✅ التحقق من الحقول المطلوبة
- ✅ التحقق من حجم ونوع الصورة
- ✅ عرض الكتب بدون أغلفة في الصفحة الرئيسية
- ✅ عرض الكتب بدون أغلفة في قائمة الأمنيات
- ✅ إرسال إشعار للجميع
- ✅ إرسال إشعار للمشترين فقط
- ✅ إرسال إشعار للمسوقين فقط
- ✅ معالجة الأخطاء في جميع الحالات

---

## 9. الملاحظات المهمة

1. **صورة الغلاف:**
   - غير إلزامية عند إضافة كتاب
   - إذا لم يتم رفع صورة، سيظهر للمشترين "الأغلفة قيد الرفع"
   - يمكن إضافة الصورة لاحقاً عن طريق التعديل

2. **الإشعارات:**
   - يتم إرسال الإشعارات فوراً
   - يظهر عدد المستخدمين الذين تم إرسال الإشعار لهم
   - الإشعارات تظهر في صفحة الإشعارات للمستخدمين

3. **الأمان:**
   - جميع الوظائف متاحة للإدارة فقط
   - التحقق من الصلاحيات في قاعدة البيانات
   - معالجة الأخطاء بشكل آمن

---

## 10. التحسينات المستقبلية

- [ ] رفع صور متعددة للكتاب
- [ ] معاينة الكتاب قبل النشر
- [ ] جدولة إرسال الإشعارات
- [ ] قوالب جاهزة للإشعارات
- [ ] إحصائيات عن الإشعارات (معدل الفتح، التفاعل)
- [ ] رفع كتب بشكل جماعي (Bulk Upload) من ملف Excel/CSV
- [ ] تصدير قائمة الكتب إلى Excel
