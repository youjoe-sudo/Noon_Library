# تحسينات نظام التسويق بالعمولة (Affiliate System Enhancements)

## الميزات المضافة

### 1. صفحة تسجيل المسوقين المحسّنة
- ✅ إضافة حقل "طريقة استلام الأرباح" (فودافون كاش، InstaPay، تحويل بنكي، إلخ)
- ✅ إضافة حقل "تفاصيل الدفع" (رقم المحفظة أو الحساب البنكي)
- ✅ التحقق من ملء جميع الحقول المطلوبة قبل التسجيل

### 2. نظام روابط التتبع (Tracking Links)
- ✅ إنشاء روابط تتبع فريدة لكل مسوق
- ✅ تتبع عدد النقرات على كل رابط
- ✅ تتبع عدد التحويلات (المبيعات) من كل رابط
- ✅ حساب معدل التحويل لكل رابط
- ✅ إمكانية إنشاء روابط متعددة لنفس المسوق
- ✅ نسخ الرابط بنقرة واحدة

### 3. لوحة تحكم المسوق المحسّنة
تعرض الآن:
- ✅ جميع روابط التتبع مع إحصائياتها
- ✅ قائمة الطلبات التي تمت من خلال المسوق
- ✅ حالة كل طلب (قيد الانتظار، مؤكد، قيد التحضير، تم الشحن، تم التوصيل، ملغي)
- ✅ العمولة المستحقة من كل طلب
- ✅ تاريخ كل طلب
- ✅ عدد العناصر في كل طلب

### 4. نظام إشعارات الإدارة
- ✅ إشعار تلقائي عند وصول المسوق لحد الأرباح (1000 جنيه)
- ✅ صفحة إشعارات مخصصة للإدارة
- ✅ عداد الإشعارات غير المقروءة في القائمة الجانبية
- ✅ تحديث تلقائي كل 30 ثانية
- ✅ إمكانية تحديد الإشعارات كمقروءة
- ✅ تصفية الإشعارات (الكل، غير مقروء، مقروء)

### 5. تتبع الزيارات من الروابط
- ✅ تسجيل تلقائي للنقرات عند زيارة الموقع عبر رابط مسوق
- ✅ حفظ كود المسوق في localStorage للربط بالطلب عند الشراء
- ✅ إشعار ترحيبي للزائر عند الدخول عبر رابط مسوق

## البنية التقنية

### قاعدة البيانات
تم إضافة الجداول والحقول التالية:

1. **جدول affiliates**:
   - `payment_method`: طريقة استلام الأرباح
   - `payment_details`: تفاصيل الدفع

2. **جدول affiliate_tracking**:
   - `id`: معرف فريد
   - `affiliate_id`: معرف المسوق
   - `tracking_code`: كود التتبع الفريد
   - `clicks`: عدد النقرات
   - `conversions`: عدد التحويلات
   - `created_at`: تاريخ الإنشاء
   - `last_click_at`: آخر نقرة

3. **جدول admin_notifications**:
   - `id`: معرف فريد
   - `type`: نوع الإشعار
   - `title`: عنوان الإشعار
   - `message`: نص الإشعار
   - `affiliate_id`: معرف المسوق (اختياري)
   - `order_id`: معرف الطلب (اختياري)
   - `is_read`: حالة القراءة
   - `created_at`: تاريخ الإنشاء

### الدوال (Functions)
تم إضافة الدوال التالية:

1. **generate_tracking_code**: إنشاء كود تتبع فريد
2. **record_tracking_click**: تسجيل نقرة على رابط التتبع
3. **record_tracking_conversion**: تسجيل تحويل (عملية شراء)
4. **check_affiliate_earnings_threshold**: التحقق من وصول المسوق لحد الأرباح

### API Functions
تم إضافة الدوال التالية في `src/db/api.ts`:

- `generateTrackingCode`: إنشاء كود تتبع جديد
- `getAffiliateTrackingLinks`: الحصول على روابط التتبع للمسوق
- `recordTrackingClick`: تسجيل نقرة
- `recordTrackingConversion`: تسجيل تحويل
- `getAffiliateOrders`: الحصول على طلبات المسوق
- `getAdminNotifications`: الحصول على إشعارات الإدارة
- `getUnreadAdminNotificationsCount`: عدد الإشعارات غير المقروءة
- `markAdminNotificationAsRead`: تحديد إشعار كمقروء
- `markAllAdminNotificationsAsRead`: تحديد جميع الإشعارات كمقروءة

## الصفحات المحدّثة

1. **AffiliateRegister.tsx**: إضافة حقول طريقة الدفع
2. **AffiliateDashboard.tsx**: عرض روابط التتبع والطلبات
3. **Home.tsx**: تتبع الزيارات من روابط المسوقين
4. **AdminLayout.tsx**: عرض عداد الإشعارات
5. **AdminNotifications.tsx**: صفحة جديدة لإدارة الإشعارات

## كيفية الاستخدام

### للمسوقين:
1. التسجيل في النظام مع تحديد طريقة استلام الأرباح
2. إنشاء روابط تتبع من لوحة التحكم
3. مشاركة الروابط مع العملاء
4. متابعة الإحصائيات والطلبات من اللوحة

### للإدارة:
1. مراجعة الإشعارات من القائمة الجانبية
2. الموافقة على طلبات السحب عند وصول المسوق للحد الأدنى
3. متابعة أداء المسوقين من صفحة إدارة المسوقين

## الأمان والصلاحيات

- ✅ المسوقون يمكنهم رؤية روابطهم وطلباتهم فقط
- ✅ الإدارة فقط يمكنها رؤية جميع البيانات والإشعارات
- ✅ تسجيل النقرات متاح للجميع (للتتبع العام)
- ✅ جميع العمليات محمية بسياسات RLS في Supabase

## ملاحظات مهمة

1. **حد الأرباح**: الحد الافتراضي للإشعار هو 1000 جنيه، يمكن تعديله من الدالة `check_affiliate_earnings_threshold`
2. **تحديث الإشعارات**: يتم تحديث عداد الإشعارات تلقائياً كل 30 ثانية
3. **حفظ كود المسوق**: يتم حفظ كود المسوق في localStorage عند الزيارة، ويجب ربطه بالطلب عند الشراء
4. **معدل التحويل**: يتم حسابه تلقائياً من (التحويلات ÷ النقرات × 100)
