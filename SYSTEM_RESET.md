# إعادة تعيين النظام (System Reset)

## نظرة عامة
تم مسح جميع البيانات التجريبية وإعداد النظام لبدء جديد مع جعل أول مستخدم مديراً تلقائياً.

---

## 1. مسح البيانات التجريبية

### البيانات التي تم مسحها:
- ✅ جميع الطلبات (Orders)
- ✅ جميع عناصر الطلبات (Order Items)
- ✅ جميع إيصالات الدفع (Payment Receipts)
- ✅ جميع عناصر السلة (Cart Items)
- ✅ جميع قوائم الأمنيات (Wishlist)
- ✅ جميع الإشعارات (Notifications)
- ✅ جميع إشعارات الإدارة (Admin Notifications)
- ✅ جميع تتبع المسوقين (Affiliate Tracking)
- ✅ جميع روابط المسوقين (Affiliate Links)
- ✅ جميع استخدامات الكوبونات (Coupon Usage)
- ✅ جميع الكوبونات (Coupons)
- ✅ جميع المسوقين (Affiliates)
- ✅ جميع الكتب (Books)
- ✅ جميع العناوين (Addresses)
- ✅ جميع قواعد الشحن (Shipping Rules)
- ✅ جميع المستخدمين (Profiles)

### الحالة الحالية:
```sql
profiles_count: 0
books_count: 0
orders_count: 0
notifications_count: 0
affiliates_count: 0
```

---

## 2. نظام أول مستخدم كمدير

### كيفية العمل:
عند تسجيل أول مستخدم في النظام:
1. يتم إنشاء حساب في `auth.users`
2. يتم تشغيل trigger `on_auth_user_created`
3. يتم إنشاء ملف تعريف في `profiles` مع:
   - `id`: نفس معرف المستخدم من auth
   - `email`: البريد الإلكتروني
   - `username`: الجزء قبل @ من البريد
   - `role`: **admin** (للمستخدم الأول فقط)

### المستخدمون التاليون:
- جميع المستخدمين بعد الأول يحصلون على دور `customer` (مستخدم عادي)
- يمكن للمدير تغيير أدوار المستخدمين من لوحة الإدارة

### الكود التقني:
```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER AS $$
DECLARE
  user_count INT;
BEGIN
  -- عد المستخدمين الموجودين
  SELECT COUNT(*) INTO user_count FROM auth.users;
  
  -- إدراج ملف تعريف
  INSERT INTO public.profiles (id, email, username, role)
  VALUES (
    NEW.id,
    NEW.email,
    SPLIT_PART(NEW.email, '@', 1),
    -- أول مستخدم = مدير، الباقي = مستخدمين
    CASE WHEN user_count <= 1 THEN 'admin'::public.user_role 
         ELSE 'customer'::public.user_role END
  );
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

---

## 3. إصلاح صفحة الملف الشخصي

### المشكلة السابقة:
كانت صفحة الملف الشخصي تعرض "غير متوفر" لجميع الحقول.

### الحل:
تم تحديث صفحة Profile لعرض البيانات بشكل صحيح:

#### اسم المستخدم:
```typescript
profile?.username || user?.email?.split('@')[0] || 'غير متوفر'
```
- يحاول أولاً الحصول على username من profile
- إذا لم يكن متوفراً، يستخرج الجزء قبل @ من email
- إذا فشل كل شيء، يعرض "غير متوفر"

#### البريد الإلكتروني:
```typescript
profile?.email || user?.email || 'غير متوفر'
```
- يحاول أولاً الحصول على email من profile
- إذا لم يكن متوفراً، يستخدم email من auth.user
- إذا فشل كل شيء، يعرض "غير متوفر"

#### الدور:
```typescript
profile?.role === 'admin' ? 'مدير' : 
profile?.role === 'affiliate' ? 'مسوق' : 
'مستخدم'
```
- مدير: للمستخدمين بدور admin
- مسوق: للمستخدمين بدور affiliate
- مستخدم: للمستخدمين بدور customer

---

## 4. خطوات البدء

### الخطوة 1: إنشاء حساب المدير
1. اذهب إلى صفحة التسجيل
2. أدخل اسم المستخدم (مثال: `admin`)
3. أدخل كلمة المرور
4. اضغط "تسجيل"
5. **سيتم تعيينك تلقائياً كمدير** 🎉

### الخطوة 2: التحقق من الدور
1. اذهب إلى "الملف الشخصي"
2. تحقق من أن الدور يظهر: **مدير**
3. تحقق من ظهور اسم المستخدم والبريد الإلكتروني بشكل صحيح

### الخطوة 3: الوصول إلى لوحة الإدارة
1. اضغط على "لوحة الإدارة" في القائمة
2. ستظهر لك جميع أدوات الإدارة:
   - إدارة الكتب
   - إدارة الطلبات
   - إدارة المسوقين
   - الإشعارات
   - التحليلات

### الخطوة 4: إضافة الكتب
1. اذهب إلى "إدارة الكتب"
2. اضغط "إضافة كتاب جديد"
3. املأ البيانات:
   - صورة الغلاف (اختياري)
   - العنوان
   - المؤلف
   - التصنيف
   - السعر
   - الكمية
4. اضغط "إضافة"

---

## 5. إنشاء مستخدمين جدد

### مستخدم عادي:
1. قم بتسجيل الخروج من حساب المدير
2. اذهب إلى صفحة التسجيل
3. سجل حساب جديد
4. سيحصل على دور **مستخدم** (customer)

### مسوق:
1. المستخدم العادي يسجل الدخول
2. يذهب إلى صفحة "انضم كمسوق"
3. يملأ نموذج التسجيل كمسوق
4. المدير يوافق على الطلب من لوحة الإدارة
5. يتم تغيير دور المستخدم إلى **مسوق** (affiliate)

---

## 6. الأدوار والصلاحيات

### المدير (Admin):
- ✅ الوصول إلى لوحة الإدارة الكاملة
- ✅ إدارة الكتب (إضافة، تعديل، حذف)
- ✅ إدارة الطلبات
- ✅ إدارة المسوقين
- ✅ إرسال الإشعارات
- ✅ عرض التحليلات والإحصائيات
- ✅ الموافقة على طلبات المسوقين
- ✅ إدارة إيصالات الدفع

### المسوق (Affiliate):
- ✅ لوحة تحكم المسوق
- ✅ إنشاء روابط تسويقية
- ✅ عرض الإحصائيات والأرباح
- ✅ طلب سحب الأرباح
- ✅ تصفح وشراء الكتب

### المستخدم (Customer):
- ✅ تصفح الكتب
- ✅ إضافة إلى السلة
- ✅ إضافة إلى قائمة الأمنيات
- ✅ إتمام الطلبات
- ✅ عرض الطلبات السابقة
- ✅ إدارة العناوين
- ✅ التقديم لتصبح مسوق

---

## 7. البيانات المطلوبة للبدء

### الكتب:
يجب على المدير إضافة الكتب يدوياً من لوحة الإدارة.

**الحقول المطلوبة:**
- العنوان بالعربية *
- المؤلف بالعربية *
- التصنيف *
- السعر *
- الكمية المتوفرة *

**الحقول الاختيارية:**
- صورة الغلاف
- الوصف
- سعر الخصم
- نسبة عمولة المسوقين (افتراضي: 10%)

### التصنيفات المتاحة:
- أدب
- علوم
- تاريخ
- فلسفة
- دين
- تنمية بشرية
- أطفال
- روايات
- شعر
- سياسة
- اقتصاد
- تكنولوجيا

---

## 8. الاختبار

### اختبار نظام أول مستخدم كمدير:
```bash
# 1. تأكد من مسح جميع المستخدمين
SELECT COUNT(*) FROM auth.users;  -- يجب أن يكون 0

# 2. سجل مستخدم جديد من الواجهة
# اسم المستخدم: admin
# كلمة المرور: (اختر كلمة مرور قوية)

# 3. تحقق من الدور
SELECT username, email, role FROM profiles WHERE username = 'admin';
-- يجب أن يظهر role = 'admin'

# 4. سجل مستخدم ثاني
# اسم المستخدم: user1
# كلمة المرور: (اختر كلمة مرور)

# 5. تحقق من الدور
SELECT username, email, role FROM profiles WHERE username = 'user1';
-- يجب أن يظهر role = 'customer'
```

### اختبار صفحة الملف الشخصي:
1. سجل الدخول بأي حساب
2. اذهب إلى "الملف الشخصي"
3. تحقق من ظهور:
   - ✅ اسم المستخدم (مثال: admin)
   - ✅ البريد الإلكتروني (مثال: admin@miaoda.com)
   - ✅ الدور (مثال: مدير)

---

## 9. الأمان

### حماية لوحة الإدارة:
- جميع صفحات الإدارة محمية بـ RLS (Row Level Security)
- فقط المستخدمون بدور `admin` يمكنهم الوصول
- يتم التحقق من الصلاحيات في قاعدة البيانات

### حماية البيانات:
- جميع الجداول محمية بسياسات RLS
- المستخدمون يمكنهم فقط الوصول إلى بياناتهم الخاصة
- المدير يمكنه الوصول إلى جميع البيانات

### كلمات المرور:
- يتم تشفير كلمات المرور بواسطة Supabase Auth
- لا يتم تخزين كلمات المرور في قاعدة البيانات
- يتم استخدام bcrypt للتشفير

---

## 10. استكشاف الأخطاء

### المشكلة: لا يظهر اسم المستخدم في الملف الشخصي
**الحل:**
- تأكد من تسجيل الدخول بنجاح
- تحقق من أن trigger `on_auth_user_created` يعمل
- تحقق من وجود بيانات في جدول profiles

### المشكلة: المستخدم الأول ليس مديراً
**الحل:**
- تأكد من مسح جميع المستخدمين قبل التسجيل
- تحقق من أن trigger يعمل بشكل صحيح
- يمكن تغيير الدور يدوياً:
```sql
UPDATE profiles SET role = 'admin' WHERE username = 'admin';
```

### المشكلة: لا يمكن الوصول إلى لوحة الإدارة
**الحل:**
- تأكد من أن دورك هو `admin`
- تحقق من تسجيل الدخول بنجاح
- امسح الـ cache وأعد تحميل الصفحة

---

## 11. الملاحظات المهمة

1. **أول مستخدم فقط:**
   - فقط أول مستخدم يسجل في النظام يحصل على دور المدير
   - جميع المستخدمين التاليين يحصلون على دور customer

2. **البريد الإلكتروني:**
   - يتم إنشاء بريد إلكتروني تلقائي: `username@miaoda.com`
   - هذا البريد داخلي فقط ولا يتم استخدامه للإرسال

3. **تغيير الأدوار:**
   - المدير يمكنه تغيير أدوار المستخدمين من لوحة الإدارة
   - لا يمكن للمستخدمين تغيير أدوارهم بأنفسهم

4. **النسخ الاحتياطي:**
   - يُنصح بعمل نسخة احتياطية من قاعدة البيانات بشكل دوري
   - يمكن استخدام Supabase Dashboard لعمل النسخ الاحتياطية

---

## 12. الخطوات التالية

بعد إنشاء حساب المدير:
1. ✅ إضافة الكتب
2. ✅ إعداد قواعد الشحن (إذا لزم الأمر)
3. ✅ إنشاء كوبونات خصم (اختياري)
4. ✅ دعوة المسوقين
5. ✅ بدء استقبال الطلبات

---

## 13. الدعم

إذا واجهت أي مشاكل:
1. تحقق من console logs في المتصفح
2. تحقق من Supabase logs
3. راجع هذا الملف للحلول الشائعة
4. تحقق من أن جميع migrations تم تطبيقها بنجاح
