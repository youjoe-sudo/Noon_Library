# نظام دفع العربون ومراجعة الإيصالات (Payment Deposit & Receipt Review System)

## نظرة عامة
تم تطوير نظام متكامل لدفع العربون ومراجعة إيصالات الدفع من قبل الإدارة، مع إشعارات تلقائية للعملاء.

## سير العمل (Workflow)

### 1. إنشاء الطلب (Order Creation)
- عند إتمام عملية الشراء من صفحة Checkout
- يتم إنشاء الطلب بحالة `pending_payment` (انتظار الدفع)
- يتم حساب العربون = تكلفة الشحن
- يتم التوجيه تلقائياً لصفحة دفع العربون

### 2. صفحة دفع العربون (Payment Deposit Page)
**المسار:** `/payment/:orderId`

**المحتويات:**
- ✅ ملخص الطلب (رقم الطلب، الإجمالي، تكلفة الشحن، العربون المطلوب)
- ✅ معلومات التحويل (رقم فودافون كاش / InstaPay / حساب بنكي)
- ✅ شروط وأحكام الدفع
- ✅ نموذج رفع الإيصال:
  - رقم الهاتف المحول منه
  - رقم الإيصال / رقم التحويل
  - صورة الإيصال (حد أقصى 1 ميجابايت)
  - معاينة الصورة قبل الإرسال

**بعد الإرسال:**
- يتم رفع الصورة إلى Supabase Storage (bucket: receipts)
- يتم إنشاء سجل في جدول `payment_receipts`
- يتم تحديث حالة الطلب إلى `awaiting_review` (قيد المراجعة)
- يتم التوجيه لصفحة الطلبات

### 3. مراجعة الإيصالات (Admin Receipt Review)
**الصفحة:** Admin Orders (`/admin/orders`)

**الميزات:**
- ✅ زر "عرض الإيصال" يظهر للطلبات في حالة `awaiting_review` أو `pending_payment`
- ✅ مربع حوار يعرض:
  - رقم الهاتف المحول منه
  - رقم الإيصال
  - تاريخ الإرسال
  - صورة الإيصال (بحجم كبير للمراجعة)
  - حقل ملاحظات الإدارة
  - أزرار الموافقة / الرفض

**عند الموافقة:**
- يتم تحديث حالة الإيصال إلى `approved`
- يتم تحديث حالة الطلب إلى `confirmed` (مؤكد)
- يمكن للإدارة إضافة ملاحظات اختيارية

**عند الرفض:**
- يتم تحديث حالة الإيصال إلى `rejected`
- يتم تحديث حالة الطلب إلى `payment_rejected` (رُفض الدفع)
- **إلزامي:** يجب كتابة سبب الرفض
- يتم إنشاء إشعار تلقائي للعميل في جدول `notifications`

### 4. إشعارات العملاء (Customer Notifications)
**عند رفض الإيصال:**
- يتم إرسال إشعار للعميل يحتوي على:
  - العنوان: "تم رفض إيصال الدفع"
  - الرسالة: "عذراً، تم رفض إيصال الدفع للطلب #[رقم الطلب]. السبب: [سبب الرفض]"
  - نوع الإشعار: `payment_rejected`
  - معرف الطلب المرتبط

### 5. صفحة الطلبات للعملاء (Customer Orders Page)
**الميزات المضافة:**
- ✅ عرض جميع حالات الطلبات الجديدة:
  - معلق (pending)
  - انتظار الدفع (pending_payment)
  - قيد المراجعة (awaiting_review)
  - رُفض الدفع (payment_rejected)
  - مؤكد (confirmed)
  - قيد التحضير (preparing)
  - تم الشحن (shipped)
  - تم التوصيل (delivered)
  - ملغي (cancelled)

**للطلبات في حالة `pending_payment`:**
- يظهر زر "💳 دفع العربون الآن"
- ينقل العميل لصفحة الدفع

**للطلبات في حالة `payment_rejected`:**
- يظهر تنبيه أحمر بسبب الرفض
- يظهر زر "إعادة رفع الإيصال"
- ينقل العميل لصفحة الدفع لإعادة المحاولة

## البنية التقنية

### قاعدة البيانات

#### جدول `payment_receipts`
```sql
- id: UUID (primary key)
- order_id: UUID (foreign key → orders)
- phone_from: TEXT (رقم الهاتف المحول منه)
- receipt_image_url: TEXT (رابط صورة الإيصال)
- receipt_number: TEXT (رقم الإيصال)
- admin_notes: TEXT (ملاحظات الإدارة)
- status: ENUM ('pending', 'approved', 'rejected')
- reviewed_by: UUID (foreign key → profiles)
- reviewed_at: TIMESTAMPTZ
- created_at: TIMESTAMPTZ
- updated_at: TIMESTAMPTZ
```

#### تحديثات جدول `orders`
```sql
- deposit_amount: DECIMAL(10, 2) (مبلغ العربون)
- status: تم إضافة حالات جديدة:
  - pending
  - pending_payment
  - payment_rejected
```

#### Supabase Storage
- **Bucket:** `receipts`
- **المسار:** `{user_id}/{timestamp}.{ext}`
- **الصلاحيات:**
  - المستخدمون يمكنهم رفع إيصالاتهم فقط
  - الجميع يمكنهم عرض الإيصالات
  - الإدارة فقط يمكنها حذف الإيصالات

### الدوال (Functions)

#### `approve_payment_receipt(receipt_id, admin_id, notes?)`
- تحديث حالة الإيصال إلى `approved`
- تحديث حالة الطلب إلى `confirmed`
- حفظ ملاحظات الإدارة (اختياري)
- تسجيل معرف المراجع وتاريخ المراجعة

#### `reject_payment_receipt(receipt_id, admin_id, notes)`
- تحديث حالة الإيصال إلى `rejected`
- تحديث حالة الطلب إلى `payment_rejected`
- حفظ سبب الرفض (إلزامي)
- إنشاء إشعار تلقائي للعميل
- تسجيل معرف المراجع وتاريخ المراجعة

### API Functions (src/db/api.ts)

```typescript
// إنشاء إيصال دفع
createPaymentReceipt(receipt: {
  order_id: string;
  phone_from: string;
  receipt_image_url: string;
  receipt_number: string;
})

// الحصول على إيصال بواسطة معرف الطلب
getPaymentReceiptByOrderId(orderId: string)

// رفع صورة الإيصال
uploadReceiptImage(file: File, userId: string)

// الموافقة على الإيصال (إدارة فقط)
approvePaymentReceipt(receiptId: string, adminId: string, notes?: string)

// رفض الإيصال (إدارة فقط)
rejectPaymentReceipt(receiptId: string, adminId: string, notes: string)

// الحصول على جميع الإيصالات (إدارة فقط)
getAllPaymentReceipts(status?: string)
```

### الصفحات المضافة/المحدثة

#### صفحات جديدة:
1. **PaymentDeposit.tsx** (`/payment/:orderId`)
   - صفحة دفع العربون ورفع الإيصال

#### صفحات محدثة:
1. **Checkout.tsx**
   - إضافة حقل `deposit_amount` عند إنشاء الطلب
   - تعيين حالة الطلب إلى `pending_payment`
   - التوجيه لصفحة الدفع بدلاً من صفحة الطلبات

2. **AdminOrders.tsx**
   - إضافة زر "عرض الإيصال"
   - مربع حوار مراجعة الإيصال
   - أزرار الموافقة/الرفض
   - تحديث قائمة الحالات

3. **Orders.tsx**
   - تحديث badges الحالات
   - إضافة زر "دفع العربون الآن"
   - إضافة تنبيه رفض الدفع
   - زر "إعادة رفع الإيصال"

### الأنواع (Types)

```typescript
export type OrderStatus = 
  | 'pending' 
  | 'pending_payment' 
  | 'awaiting_review' 
  | 'payment_rejected' 
  | 'confirmed' 
  | 'preparing' 
  | 'shipped' 
  | 'delivered' 
  | 'cancelled';

export type PaymentReceiptStatus = 'pending' | 'approved' | 'rejected';

export interface PaymentReceipt {
  id: string;
  order_id: string;
  phone_from: string;
  receipt_image_url: string;
  receipt_number: string;
  admin_notes: string | null;
  status: PaymentReceiptStatus;
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  updated_at: string;
}

export interface Order {
  // ... الحقول الموجودة
  deposit_amount: number;
  payment_receipts?: PaymentReceipt[];
}
```

## الأمان والصلاحيات (Security & Permissions)

### Row Level Security (RLS)

#### جدول `payment_receipts`:
- ✅ المستخدمون يمكنهم رؤية إيصالاتهم فقط
- ✅ المستخدمون يمكنهم إنشاء إيصالات لطلباتهم فقط
- ✅ الإدارة يمكنها رؤية جميع الإيصالات
- ✅ الإدارة يمكنها تحديث جميع الإيصالات

#### Supabase Storage (bucket: receipts):
- ✅ المستخدمون يمكنهم رفع إيصالاتهم فقط (في مجلد user_id الخاص بهم)
- ✅ الجميع يمكنهم عرض الإيصالات (للمراجعة)
- ✅ الإدارة فقط يمكنها حذف الإيصالات

## التحقق من الصحة (Validation)

### صفحة دفع العربون:
- ✅ التحقق من نوع الملف (صور فقط)
- ✅ التحقق من حجم الملف (حد أقصى 1 ميجابايت)
- ✅ التحقق من ملء جميع الحقول
- ✅ معاينة الصورة قبل الإرسال

### مراجعة الإيصال (الإدارة):
- ✅ عند الرفض: يجب كتابة سبب الرفض (إلزامي)
- ✅ عند الموافقة: الملاحظات اختيارية

## تجربة المستخدم (User Experience)

### للعملاء:
1. إتمام الطلب → توجيه تلقائي لصفحة الدفع
2. رفع الإيصال → إشعار بالنجاح → توجيه لصفحة الطلبات
3. في حالة الرفض → إشعار بالسبب → إمكانية إعادة المحاولة
4. في حالة الموافقة → تحديث حالة الطلب إلى "مؤكد"

### للإدارة:
1. عرض جميع الطلبات في صفحة واحدة
2. زر واضح لعرض الإيصالات
3. مربع حوار كبير لمراجعة الإيصال
4. أزرار واضحة للموافقة/الرفض
5. حقل ملاحظات لتوثيق القرار

## الإشعارات (Notifications)

### إشعار رفض الدفع:
- **النوع:** `payment_rejected`
- **العنوان:** "تم رفض إيصال الدفع"
- **الرسالة:** "عذراً، تم رفض إيصال الدفع للطلب #[رقم الطلب]. السبب: [سبب الرفض]"
- **الإجراء:** يمكن للعميل النقر على الإشعار للذهاب للطلب
- **التوقيت:** فوري عند رفض الإيصال

## ملاحظات مهمة

1. **العربون = تكلفة الشحن:** يتم حساب العربون تلقائياً بناءً على تكلفة الشحن
2. **الدفع عند الاستلام:** باقي المبلغ يُدفع عند استلام الطلب
3. **مراجعة خلال 24 ساعة:** يتم إشعار العميل بأن المراجعة تتم خلال 24 ساعة
4. **إعادة المحاولة:** في حالة الرفض، يمكن للعميل إعادة رفع الإيصال
5. **حفظ السجلات:** يتم حفظ جميع الإيصالات والملاحظات للرجوع إليها

## الاختبار (Testing)

### سيناريوهات الاختبار:
1. ✅ إنشاء طلب جديد → التوجيه لصفحة الدفع
2. ✅ رفع إيصال صحيح → تحديث الحالة إلى "قيد المراجعة"
3. ✅ محاولة رفع ملف كبير → رسالة خطأ
4. ✅ محاولة رفع ملف غير صورة → رسالة خطأ
5. ✅ الإدارة توافق على الإيصال → تحديث الحالة إلى "مؤكد"
6. ✅ الإدارة ترفض الإيصال → إنشاء إشعار للعميل
7. ✅ العميل يعيد رفع الإيصال بعد الرفض → تحديث السجل

## التطويرات المستقبلية

- [ ] التحقق التلقائي من الإيصالات باستخدام AI
- [ ] إشعارات SMS للعملاء
- [ ] تكامل مع بوابات الدفع الإلكتروني (Stripe, Paymob)
- [ ] لوحة تحكم للإحصائيات (معدل الموافقة/الرفض)
- [ ] تصدير تقارير الإيصالات (PDF, Excel)
